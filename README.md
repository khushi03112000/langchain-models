          step 1 : python3 -m venv venv           => creates virtual env            
          source venv/bin/activate           => activates virtual env 

          step 2 : make requirements.txt file and all all the langchain components
          step 3 : run : pip install --upgrade pip
          step 4 : run : pip install -r requiremnents.txt
          step 5 : run : python test.py  => to test langchain installed correctly
